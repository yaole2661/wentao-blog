# 复制本文件为 .env 并填写后，再执行 docker compose up -d
# 生成随机密钥：
#   Windows PowerShell: openssl rand -hex 32   （或 [guid]::NewGuid().ToString('N').Substring(0,32)）
#   Linux / macOS:      openssl rand -hex 32

# Umami 会话加密密钥（必填，随机 32+ 字符）
APP_SECRET=

# Postgres 数据库密码（必填，随机强密码）
POSTGRES_PASSWORD=