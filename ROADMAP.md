---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: '9bcc6d88-3dfe-4a45-9c32-93dd99bddc58'
  PropagateID: '9bcc6d88-3dfe-4a45-9c32-93dd99bddc58'
  ReservedCode1: '4f1b5c6b-e0cd-4df0-b619-339b4a223fcd'
  ReservedCode2: '4f1b5c6b-e0cd-4df0-b619-339b4a223fcd'
---

# 博客运维路线图（本地）

> 不发布到站点，仅本地跟踪。原 `src/content/note/blog-roadmap.md` 已下线。
> 关联策略文档见 `docs/`：
> - 内容侧：`博客选题建议.md`、`社交平台联动策略方案.md`
> - 工程侧：`网站设计与架构评估.md`、`补充优化与可选事项.md`
> - 对标学习：`对标GatesNotes与75live学习点.md`、`对标ainoteatlas信息架构对比.md`、`对标ainoteatlas版式设计对比.md`、`对标ainoteatlas改造建议.md`
> - 运维：`图片优化与带宽策略.md`
> - 追溯：`变更记录.md`（代码改动台账，与本路线图待办同步）

## 一、发布规则（重要）

- **新生成的 `src/content/` 下的 MD 默认 `draft: true`**，不会出现在任何线上页面。
- 只有用户明确提出"发布"时，才把 `draft` 改为 `false`（或删除该字段）。
- 线上现有 6 篇均为建站期 AI 占位内容，处置方式由站主决定（保留占位 / 转草稿 / 删除）。

---

## 二、已完成留档 ✅

### P0 上线前 ✅ 全部完成

- ✅ URL 同改 `https://fanwentao.cn` + 署名统一 Wentao + robots.txt
- ✅ git push → Vercel 项目创建 → Discussions + giscus → COMMENTS.enabled = true
- ✅ NS 从 DNSPod 切到 Vercel → DNS 刷新 → SSL 签发 → 正式上线
- ✅ Vercel 已连接 GitHub，push 自动部署已验证

### P1 网站建设 ✅ 已收官（2026-09-08）

- ✅ 404 与死链自测（11 页全部通过）、自定义 404 页
- ✅ RSS 2.0 校验通过（含 pubDate / category / author）
- ✅ 手机适配修复（横向溢出、长 URL 换行、悬浮目录/前后导航 dock、汉堡菜单）
- ✅ 站内搜索（search.json 前端过滤）、giscus 评论、深浅色 + 防 FOUC 主题切换
- ✅ 图片懒加载（loading=lazy）、HSTS、外链二次确认弹窗、TOC 滚动高亮
- ✅ pandoc 装好，Word→MD/视频嵌入链路可用（说明见 `src/content/note/content-workflow.md`，保持草稿）
- ⏳ 提交 sitemap 到 Google Search Console / Bing（已沉到 P0-4 一起做，见下）

---

## 三、内容创作主线（贯穿始终，持续节奏）

> 网站建设告一段落，今后以**真实内容创作驱动**。AI 只协助格式转换、润色、配图；新文章一律默认草稿，站主亲自定稿。

**定位一句话**：一个非科班产品经理，亲手搭出真正跑起来的 AI 智能体。（详见 `docs/博客选题建议.md`）

**内容优先级**（详细选题见 `docs/博客选题建议.md`）：

| 梯队 | 内容 | 节奏 |
| --- | --- | --- |
| 开场 | 《我是谁：非科班 PM 正在亲手搭一个 AI 智能体》 | 第 1 篇，四平台自我介绍母版 |
| A（实战复盘） | 订单确认卡决策、多运力接口对齐、A2UI 双版本 PRD、协作 SOP | 每周 1 篇，连发 4 篇 |
| B（方法论） | 智能体验收、自然语言编程、AI 中后台设计 | 每月 1–2 篇 |
| C（多元） | CNS 月度串烧、企业×AI 交叉观察 | 维持双线人设 |

**关键判断**：1 篇正式文 > 6 篇 AI 草稿。先质量后数量，实名文 800–1500 字即可。

**社交联动**（四个平台：公众号/知乎/小红书/小宇宙）流水线：一篇文章源 → 五形态分发，回链博客，全程埋 UTM。详见 `docs/社交平台联动策略方案.md`。

---

## 三·五、内容架构升级（对标 ainoteatlas · 与 P0 并行）

> 来源：`docs/对标ainoteatlas改造建议.md`（最完整）+ `信息架构对比` + `版式设计对比` 三份。核心结论：现在 6 篇是「建结构成本最低的时刻」，等 60 篇再重构代价×10。真正该抄的不是配色，是「方向 → 专题 → 文章」三层骨架 + 「每篇带时长与序列位置」的元信息纪律。

**一句话**：内容体系已存在（企业研究/前沿 AI 双主线），缺的是把它从「流水账」翻译成「地图」的那层目录。差异化武器 = 企业研究这条线（ainoteatlas 纯 AI 工程，没有「以企业为单位的长期档案」）。

| 优先级 | 动作 | 说明 |
|---|---|---|
| 内容-P0 | 补 `readingTime` + `series`/`order` frontmatter，全站展示 | 阅读时长用 remark 插件自动算；成果=列表项 `分类 · 日期 · 12 min · 第 3/12 篇` |
| 内容-P0 | 建「研究看板」/`research/` | 中国/海外双栏 × 行业分组，每企业一张卡片；全站最有辨识度的一页 |
| 内容-P0 | 治理标签 22 → 受控词表 | 拆「实体标签」（LVMH/华为/茶思屋）与「主题标签」（≤10 个），删 #茶思屋热点# 这类组合标签 |
| 内容-P1 | URL 三层化 + 文章页顶部导航/底部连接 | `/research/` → `/series/<slug>/` → `/posts/<slug>/`；长文必加目录 |
| 内容-P1 | 企业研究「结论/证据/存疑」三段式 | 迁移自 ainoteatlas「证据边界声明」，对企业研究价值更高 |
| 内容-P1 | 首页改「入口页」 + 研究日志页 | Hero 定位宣言（底子要硬表达要软）+ 三入口卡片 + 最近 5 篇 |
| 内容-P2 | 企业档案页 / 横向对比 / 关系图谱 / 纸感视觉 | 关系图谱内容<50 篇慎做；纸感=衬线标题+暖白底+低饱和砖红 |

> 注意与现有「P0 增长基础设施」的区分：**增长 P0 是"被看见/被搜到/被追踪"的底座；内容架构 P0 是"内容组织层"的升级。** 两者可并行，不互斥。

---

## 四、P0 · 增长基础设施（当前最优先，直接卡增长）

> 来自 `docs/网站设计与架构评估.md` + `docs/补充优化与可选事项.md` 的实测缺口。这些做完，内容才有"被看见、被搜到、被追踪"的底座。

### P0-1 统一域名规范（裸域 vs www 不一致）⚠️ 实测新发现

- **现状**：代码声明裸域（`astro.config.mjs` 的 `site` 与 `src/site.ts` 的 `SITE.url` 均为 `https://fanwentao.cn`），但 Vercel 实际把裸域 `308` 跳转到 `https://www.fanwentao.cn/`。canonical / sitemap / robots / RSS 声明的是裸域，实际落地是 www —— 搜索引擎权重会被稀释、可能重复收录。
- **修复（推荐方向①）**：Vercel 后台把主域名设为裸域 `fanwentao.cn`，让 `www` 反跳裸域。代码不用动（已是裸域）。
- **2026-09-09 决策**：采纳方向①。2026-09-09 实测确认现状为裸域 308→www（www 返回 200）。待 Vercel 后台调整后重验。
- 方向②（备选）：Vercel 保持现状，改代码两处 `site`/`url` 为 `https://www.fanwentao.cn/` 并重新部署。
- 原则：**部署重定向方向** 与 **`site` 字段** 必须一致。

### P0-2 分享图 SVG → PNG（社交平台抓不到）

- **现状**：`og:image` / `twitter:image` 全部指向 `/covers/xxx.svg`。**微信/微博/QQ/知乎/小红书分享卡片不渲染 SVG**，只认 PNG/JPG，导致分享出去无缩略图。
- **修复（2026-09-10 已完成）**：新增 `scripts/generate-og-images.mjs`（`npm run og`），用 sharp 把站点默认 `og.svg` + 6 张封面统一光栅化为 1200×630 PNG，输出 `public/og.png` 与 `public/covers/og/*.png`。`BaseLayout.astro` 的 og:image 已改指 PNG 版本。构建验证通过。
- 微信还需单独配公众号 `share_img`（公众号这路不完全读 og:image）——**待做**。

### P0-3 访问统计（Umami）+ 全站埋 UTM

- **现状**：无任何统计，不知道流量从哪来、哪篇被读 —— 社交联动无法校准。
- **动作**：部署 Umami（自托管，免 Cookie 合规负担）→ `Layout.astro` 加 `<script data-website-id=...>`；所有社交引流链接统一带 `utm_source`（wechat/zhihu/xhs/xiaoyuzhou）。
- 这也是 `docs/社交平台联动策略方案.md` 闭环优化的数据前提。

### P0-4 主动提交中文搜索引擎 + 主动推送

- **现状**：sitemap/robots 都对，但从未主动提交。中文站命门：百度对新域名个人站收录极慢，不主动推可能几个月查无此站。
- **动作**：
  - 百度搜索资源平台：验证站点 → 提交 `sitemap-index.xml` → 拿「主动推送」token，构建完 `curl` 推一次。
  - Google Search Console + Bing Webmaster：提交 sitemap（顺手，原 P1 遗留项一并归此）。

---

## 五、P1 · 品牌与生态（性价比高，建立作者实体）

### P1-1 结构化数据补全
- 补 `Person` + `sameAs`（把知乎/小红书/B站/微博等主页塞进 sameAs，让搜索把"Wentao 实体"串成同一作者）。
- 补全站 `WebSite` + `SearchAction`（Google 结果页显示站内搜索框）。
- 文章页补 `BreadcrumbList`。

### P1-2 sitemap 加 lastmod
- 现状每个 URL 只有 `<loc>`，无 `<lastmod>`。用 `@astrojs/sitemap` 的 `serialize` 回调写入 `lastmod`（取文章 `date`），提升收录优先级与抓取节奏。

### P1-3 补全公众号主页链接
- `src/site.ts` 里公众号 `url: ''` 待补。公众号是私域主阵地，补上后在首页「关注」区与「关于页」生效。

### P1-4 加入中文独立博客生态 + 友链
- 提交「博友圈 / 开往 Travellings / BlogHub / 十年之约」等，冷启动期最精准的流量来源。
- 加一个 `/friends/` 友链页，与技术博客互挂。

### P1-5 版权协议标注
- 页脚/关于页标注 **CC BY-NC-SA 4.0**（署名-非商业-相同方式共享），研究类内容防无标注搬运，也方便他人合规引用。

---

## 六、P2 · 打磨与进阶（不紧急，按需）

### P2-1 系列化导航
- 即将写的「跑腿智能体」系列 4 篇，设系列页面聚合，利于追更与内链 SEO。

### P2-2 字体本地化 + 子集化
- 若引用了 Google Fonts/外链字体，国内加载慢甚至失效。下载 woff2 到 `public/fonts`，`@font-face` 引用，`font-display: swap`。

### P2-3 站点源码纳入 Git 全程管理
- 已有 `yaole2661/wentao-blog` 仓库（giscus 用）。确认主站源码也纳入 Git 版本管理，内容永远可备份、可迁移。

### 样式备选池（写文章过程中遇到再做）

| 序号 | 改进 | 难度 | 状态 |
| --- | --- | --- | --- |
| 1 | 章节编号（CSS counter，H2 前加数字） | 低 | ⏳ |
| 2 | 阅读时间估算（字数 / 300 字每分钟） | 低 | ⏳ |
| 3 | 首屏加 slogan / 价值主张 | 低 | ⏳ |
| 4 | 社交分享按钮（复制链接 + 微信二维码） | 中 | ⏳ |
| 5 | 汉堡菜单升级为全屏抽屉 | 中 | ⏳ |

---

## 七、P3 · 暂缓（等触发条件）

- 站内搜索从子串过滤升级到 Pagefind（文章数接近三位数时）。
- ICP 备案 + 国内 CDN（等知乎/搜一搜带来稳定自然流量、且国内加载体感明显拖后腿时再启动；当前 Vercel 海外节点可接受）。
- 邮箱订阅 Newsletter（公众号跑顺、有余力再上）。

---

## 八、决定不做

- Vercel Pro 升级（免费版带宽 100GB 足够）。

---

## 附：本路线图责任边界说明

- **工程侧任务**（P0/P1 的域名、分享图、统计、提交、JSON-LD 等）随时可启动，不依赖内容进度。
- **内容侧任务**（主线 + 四平台联动）由站主亲自定稿，AI 只协助。
- 两者可并行：工程侧先铺好增长底座，内容侧按节奏产出，互不阻塞。